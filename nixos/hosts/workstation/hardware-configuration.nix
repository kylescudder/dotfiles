# PLACEHOLDER ONLY.
#
# During installation run:
#   sudo nixos-generate-config --root /mnt
#
# Then replace THIS FILE with:
#   /mnt/etc/nixos/hardware-configuration.nix
#
# Do not install using this placeholder: disk filesystems, initrd modules and
# boot details are hardware-specific and cannot safely be inferred from the
# previous Arch bootstrap script.
{ ... }:
{
}
